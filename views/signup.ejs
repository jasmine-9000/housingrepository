<%- include('partials/header') -%>
    <main class="container mx-auto">
            <section class="col-6 dark:text-white p-2">
                <% if (locals.messages.errors) { %>
                    <% messages.errors.forEach( el => { %>
                        <div class="alert alert-danger"><%= el.msg %></div>
                    <% }) %>    
                <% } %>
                <% if (locals.messages.info) { %>
                    <% messages.info.forEach( el => { %>
                        <div class="alert alert-info"><%= el.msg %></div>
                    <% }) %>    
                <% } %>
                <h1 class="text-4xl font-bold mb-3">Sign up</h1>
                <form action="/signup" method="POST" class="lg:w-1/3 mb-10">
                    <div class="mb-3 flex flex-col">
                        <label for="userName" class="form-label">User Name</label>
                        <input type="text" class="form-control" id="userName" name="userName">
                      </div>
                    <div class="mb-3 flex flex-col">
                      <label for="exampleInputEmail1" class="form-label">Email address</label>
                      <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" name="email">
                      <div id="emailHelp" class="text-xs italic text-green-700 dark:text-green-400">We'll never share your email with anyone else.</div>
                    </div>
                    <div class="mb-3 flex flex-col">
                      <label for="password" class="form-label">Password</label>
                      <input type="password" class="form-control" id="password" name="password">
                    </div>
                    <div class="mb-3 flex flex-col ">
                        <label for="confirmPassword" class="form-label">Confirm Password</label>
                        <input type="password" class="form-control" id="confirmPassword" name="confirmPassword">
                      </div>
                    <button type="submit" class="btn btn-blue">Submit</button>
                  </form>
            </section>
    </main>

<%- include('partials/footer') -%>